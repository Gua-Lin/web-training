# 🚀 前端10 天训练计划

---

# 🧩 Day 1：原生登录页（理解交互本质）

## 🎯 任务

用 HTML + CSS + JS 实现登录页

### 功能

- 用户名 / 密码输入框

- 登录按钮

- 校验为空

- 判断 admin / 123456

- alert 成功/失败

---

## 📚 必学

- DOM 操作

- addEventListener

- 表单提交（submit）

- preventDefault

- git初始化项目代码仓库（git的commit\push）

---

## ✅ 验收点

- 不刷新页面完成登录逻辑

- 提供github的代码仓库链接

- 能说清：
  
  - “点击按钮后发生了什么？”
  
  - “数据是从哪里来的？”

# 🧩 Day 2-3：React 重写登录页（核心转型）

## 🎯 任务

用 React 重写 Day1

---

## 📚 技术要求

- 用 Vite 搭 React 项目

- 函数组件

- `useState`

- 受控组件

---

## 📚 必学

- 前端工程化（npm、依赖管理、脚手架、构建工具、webpack）

- react脚手架搭建（create-react-app已经过时，了解即可，当前主流是vite）

- JSX 是什么

- state 是什么

- React 事件机制

---

## ✅ 验收点

    能解释：

- 前端工程目录的定义

- package.json的常见字段含义，scripts 是干嘛的？如何修改使用

- 输入框和 state 双向绑定

- 能解释：
  
  - 为什么不用 document.querySelector？
  
  - React 和原生 JS 的区别？

---

# 🧩 Day 4：AJAX + 登录接口

## 🎯 任务

把登录改成“请求接口”

---

## 技术要求

使用：  
👉 Axios请求后端接口

遇到CORS如何处理

## 功能

- 点击登录 → 发请求

- loading 状态

- 成功：
  
  - 保存 token

- 失败：
  
  - 页面提示（不能只 alert）

启动后端服务

```bash
node server.js
```

server.js代码示例

```javascript
const http = require("http");

const server = http.createServer((req, res) => {
  // 只处理 POST /login
  if (req.method === "POST" && req.url === "/login") {
    let body = "";

    // 接收数据
    req.on("data", chunk => {
      body += chunk;
    });

    // 数据接收完毕
    req.on("end", () => {
      try {
        const data = JSON.parse(body);
        const { username, password } = data;

        if (username === "admin" && password === "123456") {
          res.writeHead(200, {
            "Content-Type": "application/json"
          });
          res.end(JSON.stringify({
            code: 0,
            data: {
              token: "node-token-123"
            }
          }));
        } else {
          res.writeHead(401, {
            "Content-Type": "application/json"
          });
          res.end(JSON.stringify({
            code: 1,
            message: "用户名或密码错误"
          }));
        }
      } catch (err) {
        res.writeHead(400);
        res.end("Bad Request");
      }
    });
  } else {
    res.writeHead(404);
    res.end("Not Found");
  }
});

server.listen(3000, () => {
  console.log("Server running at http://localhost:3000");
});
```

---

## 📚 必学

- Promise / async-await

- 请求 vs 响应

- try/catch

---

## ✅ 验收点

- 能说清：
  
  - 请求是怎么发出去的？
  
  - 为什么要 async/await？

- 页面有 loading状态

---

# 🧩 Day 5：登录态 + 路由控制

## 🎯 任务

实现页面跳转 + 登录保护

---

## 技术要求

使用：  
👉 React Router

---

## 功能

- 登录成功 → `/home`

- 未登录访问 `/home` → 跳 `/login`

---

## 📚 必学

- SPA

- 路由原理

- localStorage

---

## ✅ 验收点

- 刷新页面仍保持登录状态

- 能解释：
  
  - token 为什么存在？
  
  - 为什么前端能控制跳转？

---

# 🧩 Day 6-7：UI 组件库 + 后台页面

## 🎯 任务

做一个“后台管理页面”

---

## 技术选型

👉 Ant Design  

---

## 页面要求

- 顶部导航

- 侧边栏

- 表格

- 弹窗

- 按钮 / 输入框

---

## 📚 必学

- 查官方文档能力（重点）

- props

- 组件组合

---

## ✅ 验收点

- 能快速查文档实现组件

- 页面结构清晰

---

# 🧩 Day 8：请求封装（进阶能力）

## 🎯 任务

封装请求模块

---

## 要求

- 封装 axios 实例

- 加拦截器：
  
  - 自动带 token
  
  - 统一错误处理

---

## 📚 必学

- interceptor（拦截器）

- 为什么要封装

---

## ✅ 验收点

- 代码结构如下：

```id="3tfqjc"
src/
 ├── api/
 │    └── request.js
```

# 🧩 Day 9-10：源码阅读 （nextjs学习）

## 🎯 任务

阅读项目：

👉 ChatGPT Next Web

---

## 阅读重点

- 项目结构

- 请求封装

- react钩子

- 状态管理

- 页面组织方式

---

## 📝 输出

```id="1x7h9d"
1. 项目结构是怎样的？
2. 页面如何切换？
3. 请求是怎么封装的？
4. 哪些地方看不懂？
```

# 
